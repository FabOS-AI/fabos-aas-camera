<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">	<modelVersion>4.0.0</modelVersion>	<groupId>de.fhg.ipa.ced</groupId>	<artifactId>aas.camera</artifactId>	<version>0.0.1-SNAPSHOT</version>	<name>aas.camera</name>	<description>FabOS AAS Camera Demo</description>	<properties>		<maven.compiler.source>18</maven.compiler.source>		<maven.compiler.target>18</maven.compiler.target>		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>		<maven.plugin.codehaus.exec.version>1.2.1</maven.plugin.codehaus.exec.version>		<maven.plugin.dockerfile.version>1.4.10</maven.plugin.dockerfile.version>		<docker.image.name>fabos-aas-camera</docker.image.name>		<docker.image.registry>fabos4ai</docker.image.registry>		<docker.image.skip>false</docker.image.skip>	</properties>	<dependencies>		<dependency>			<groupId>org.eclipse.basyx</groupId>			<artifactId>basyx.components.AASServer</artifactId>			<version>1.3.1</version>		</dependency>		<dependency>			<groupId>org.eclipse.basyx</groupId>			<artifactId>basyx.components.registry</artifactId>			<version>1.3.1</version>		</dependency>		<dependency>			<groupId>org.bytedeco</groupId>			<artifactId>javacv-platform</artifactId>			<version>1.5.7</version>		</dependency>	</dependencies>	<build>		<pluginManagement>			<plugins>				<!-- Docker Image !-->				<plugin>					<groupId>com.spotify</groupId>					<artifactId>dockerfile-maven-plugin</artifactId>					<version>${maven.plugin.dockerfile.version}</version>					<executions>						<execution>							<id>build-image</id>							<goals>								<goal>build</goal>							</goals>						</execution>						<execution>							<id>push-image</id>							<phase>deploy</phase>							<goals>								<goal>push</goal>							</goals>						</execution>					</executions>					<configuration>						<useMavenSettingsForAuth>true</useMavenSettingsForAuth>						<repository>${docker.image.registry}/${docker.image.name}</repository>						<tag>${project.version}</tag>						<dockerfile>Dockerfile</dockerfile>						<contextDirectory>.</contextDirectory>						<skipDockerInfo>true</skipDockerInfo>						<skip>${docker.image.skip}</skip>					</configuration>					<dependencies>						<dependency>							<groupId>javax.activation</groupId>							<artifactId>javax.activation-api</artifactId>							<version>1.2.0</version>						</dependency>					</dependencies>				</plugin>			</plugins>		</pluginManagement>		<plugins>			<plugin>				<groupId>org.apache.maven.plugins</groupId>				<artifactId>maven-assembly-plugin</artifactId>				<version>3.6.0</version>				<executions>					<execution>						<phase>package</phase>						<goals>							<goal>single</goal>						</goals>						<configuration>							<archive>								<manifest>									<mainClass>										de.fhg.ipa.ced.aas.camera.Application									</mainClass>								</manifest>							</archive>							<descriptorRefs>								<descriptorRef>jar-with-dependencies</descriptorRef>							</descriptorRefs>						</configuration>					</execution>				</executions>			</plugin>			<!-- Docker Image !-->			<plugin>				<groupId>com.spotify</groupId>				<artifactId>dockerfile-maven-plugin</artifactId>			</plugin>		</plugins>	</build></project>